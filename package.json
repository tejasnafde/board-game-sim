{
  "name": "board-game-sim",
  "private": true,
  "version": "0.1.0",
  "description": "Browser-first board game simulator monorepo",
  "workspaces": [
    "packages/*",
    "packages/games/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  },
  "dependencies": {
    "ws": "^8.19.0"
  }
}
